version: '3.8'
services:
  frontend:
    image: nginx:alpine
    ports: ["80:80"]
    volumes: ["./:/usr/share/nginx/html"]
  java-backend:
    build: ./backend-java
  python-ai:
    build: ./ai-python
    
